Rewrite the GitHub Actions CI/CD example to use a dedicated build job
(via `build-and-inspect-python-package`) and OIDC Trusted Publishers
instead of building in publishing jobs with long-lived API tokens.
