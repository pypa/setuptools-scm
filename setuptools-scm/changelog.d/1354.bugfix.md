Wrap version in ``setuptools.sic()`` when ``normalize = false`` to prevent setuptools from re-normalizing the version after our hook returns. This preserves CalVer zero-padding (e.g. ``2024.01.05``) and other non-canonical version strings in ``dist.metadata.version``.
