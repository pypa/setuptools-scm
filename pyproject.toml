# Workspace-only configuration (no project defined at root)
[tool.uv.workspace]
members = ["./vcs-versioning", "./setuptools-scm"]

[tool.uv.sources]
vcs-versioning = { workspace = true }
setuptools-scm = { workspace = true }

